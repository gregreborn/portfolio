import 'package:flutter/cupertino.dart';

class MetaAnalysisScreen extends StatelessWidget {
  const MetaAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Meta Analysis'));
  }
}